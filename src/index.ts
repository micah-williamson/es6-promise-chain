import {_for} from './for';
import {_forLazy} from './forLazy';
import {_forEach} from './forEach';
import {_forEachLazy} from './forEachLazy';
import {_while} from './while';

export = {
  for:      _for,
  forLazy: _forLazy,
  forEach:  _forEach,
  forEachLazy: _forEachLazy,
  while:    _while
}
